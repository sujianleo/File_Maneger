# Directory Manager

一个基于 PyQt5 的极简文件夹排序器：选择目录、拖拽调整顺序，批量添加或清除数字前缀，并支持中英文界面。

## 快速开始
1. 安装依赖：
   ```bash
   pip install pyqt5
   ```
2. 在项目根目录运行：
   ```bash
   python directory_manager.py
   ```

应用会记住上次打开的路径和界面语言，配置保存在 `last_state.json`。
